name: cyclus
version: "18.0.0"
description: "hermes-cyclus — loop engineering primitives for Hermes Agent. Every skill maps to a Saturate loop kind. File-based queue (NFS-safe) with Kanban and Saturate upgrade paths."
author: Donald Thompson

provides_tools:
  - cyclus_queue
  - cyclus_evidence
